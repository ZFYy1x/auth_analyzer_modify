# V1.8 改动说明

> 迭代主题：看板数据导入/导出（JSON 快照备份与覆盖恢复）
> 日期：2026-09-03

## 一、需求背景

越权测试看板（表格）中的数据在会话结束后无法找回，复测与审计回溯时
只能重新跑一遍流量。原插件自带的「导出表格数据」只能生成 HTML/XML
**报告**（不可回读），无法恢复看板。

本次迭代新增**可回读的看板备份**：把看板全量数据（含原始请求与每个
Session 的请求/响应完整报文）导出为一个 JSON 文件，需要时再导入覆盖
恢复 —— 复测查看、审计回溯时无需重新构造流量。

## 二、功能说明

### 1. 入口

看板底部工具栏（表格配置区）新增两个按钮：

| 按钮 | 行为 |
|---|---|
| `导出看板备份` | 全量快照导出（**不受当前搜索/过滤影响**），保存为 JSON |
| `导入看板备份` | 选择备份文件 → 覆盖确认（分析运行中额外红字警告）→ 恢复 |

### 2. 备份内容（JSON，流式写入）

- 格式/版本标识：`format = AuthAnalyzerBoardBackup`，`version = 1`
- 备份时间、备份时的全部 Session 名字列表
- 每一行：`id` / `comment` / `marked` / `infoText` / `method` / `url` /
  `statusCode` / `responseContentLength` / `host` / `port` / `secure` +
  **原始请求/响应报文** + 每个 Session 的 **状态与请求/响应报文**
- 请求/响应报文一律 **Base64** 编码，保证二进制安全

### 3. 恢复语义（覆盖恢复）

- 备份数据按 **Session 名字**映射到当前 Setup 中配置的同名 Session；
  名字缺失的会话数据被跳过并弹窗列出（最多 10 个 + 总数）
- 解析与校验**全部通过后**才清空当前看板并重建；**解析失败自动中止，
  不清空现有数据**
- ID 通过 `config.getNextMapId()` 重新分配，不与后续实时流量冲突
- 恢复内容含注释、标记、绕过状态、状态码、长度、infoText 与完整报文
  → 支持右键发 Repeater 复测、在消息详情/比对视图回看响应

## 三、涉及代码

| 文件 | 改动 |
|---|---|
| `src/com/protect7/authanalyzer/util/DataExporter.java` | 新增 `SNAPSHOT_FORMAT`/`SNAPSHOT_VERSION` 常量与 `createSnapshot()`（流式 JsonWriter 全量快照）、`base64OrNull()` 帮助方法 |
| `src/com/protect7/authanalyzer/util/DataImporter.java`（新增） | `restore()` 覆盖恢复入口 + `ImportResult` 统计；流式 JsonReader 解析（`parseRow`/`parseSessionData`）；格式/版本校验；Session 按名匹配；Base64 损坏防护 |
| `src/com/protect7/authanalyzer/gui/main/CenterPanel.java` | 工具栏新增「导出看板备份」「导入看板备份」按钮；`exportBoardBackup()` / `importBoardBackup()` / `refreshTableAfterDataChange()` / `escapeHtml()`；后台线程执行 + EDT 刷新搜索索引与表格 |

## 四、验证

- 解析层单测 **19/19 通过**（反射调用，无 Burp 运行时）：字段读取、
  Base64 还原、sessionData 结构、format 不匹配、版本不支持、损坏
  Base64 防护、未知字段跳过等
- javac（`-source 8`，与 pom 编译目标一致）编译通过，无告警
- 打包产物已更新（增量写入新 class 与相关内部类）：
  - `target/auth_analyzer_modify-1.8.jar`
  - `target/auth_analyzer_modify-1.8-jar-with-dependencies.jar`（Burp 加载用）

## 五、使用方式

1. Burp → Extensions → Add，加载 `auth_analyzer_modify-1.8-jar-with-dependencies.jar`
2. 测试中/测试后点「**导出看板备份**」生成 JSON（建议随测试报告归档）
3. 复测或审计回溯时点「**导入看板备份**」，先确认当前 Setup 已存在与
   备份一致的同名 Session（Setup 自动存档，重启 Burp 后一般已就位），
   再选文件 → 确认覆盖 → 看板恢复

## 六、已知事项

- montoya 报文构造器依赖 Burp 运行时（headless 无 `ObjectFactoryLocator`
  会 NPE），「导出→导入→逐字段比对」的端到端测试无法在本地无 Burp
  环境执行；恢复层复用的是与主链路 `RequestController` 相同的
  `MontoyaUtils.requestFromBytes/responseFromBytes` 构造路径（生产已验证），
  **首次加载到 Burp 后建议实测一轮**：建 2 个 Session → 放行几条请求 →
  导出备份 → 清除表格 → 导入 → 核对行数/颜色状态/消息详情。
- 导入为覆盖语义：会清空当前看板；导入前务必确认或另存当前数据。
- 本机 `mvn` 启动器异常（plexus classworlds 类加载失败），本次采用
  `javac + ~/.m2/repository 本地依赖` 直接编译，并用 `jar uf` 增量更新
  产物（注意同步相关内部类）；后续整包构建需先修复 Maven 环境。
