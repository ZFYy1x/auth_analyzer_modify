# 2026-09-02

- 为 Auth Analyzer 看板搜索添加 {%} 通配符支持（CustomRowSorter.matchesSearch/getWildcardPattern，static + 模式缓存；CenterPanel 搜索框 placeholder/tooltip 更新）。{%} 编译为 .*，字面量 Pattern.quote 转义；路径/请求/响应三种搜索均生效，反向搜索兼容。13 个用例测试通过。
- 本机 mvn 启动器损坏（plexus classworlds 加载失败），改用 javac + ~/.m2/repository 本地依赖编译，并用 jar uf 增量更新 target 下两个 jar（注意匿名内部类 $1/$2 都要更新）。
- 迭代版本号 1.6 → 1.7：新增 `V1.7改动说明.md`（功能/语法/涉及代码/验证/已知事项），README.md 版本更新区登记 V1.7；pom.xml、BappManifest.bmf（ScreenVersion + EntryPoint）同步，target 下复制生成 1.7 两个 jar 产物。
- 将 {%} 通配符搜索功能移植到特供分支 `E:\auth_analyzer_modify_longfor`（main 分支外独立 git 仓库，版本号仍为 1.6，产物名 auth_analyzer_modify_longfor-1.6*）。两文件与主分支功能版 diff（忽略行尾）后仅余格式噪声：CenterPanel 已完全一致，CustomRowSorter 剩原有空行含 tab 与 path 块缩进差异。编译 + 16 用例测试通过后增量更新其 target 两个 jar（CustomRowSorter.class/$1/$2 + CenterPanel.class）。注意：该仓库已有 owner 本地未提交改动（RequestController/Globals/pom/BappManifest），未触碰。语义注意：`/api/{%}/html` 中的 `/` 为字面量，`{%}` 不吞分隔符，`/api/html` 不命中（与 glob 不同）。
- longfor 特供分支补全发布动作：新增 `V1.7改动说明.md`（含 longfor 特有匹配语义澄清：{%} 不吞字面量分隔符；标注该分支 16 用例验证与既有未提交改动说明），README.md 中文版本更新区新增 V1.7 章节 + 英文 Features 加 {%} 行，pom.xml/BappManifest.bmf（ScreenVersion+EntryPoint）升 1.7，target 复制生成 auth_analyzer_modify_longfor-1.7 两个 jar。
