# V1.9 改动说明

> 迭代主题：导入导出性能优化 + 看板导入可见性修复 + 清除表格二次确认
> 日期：2026-09-03

## 一、需求背景

在接口量较大的测试场景（300+ / 500+ / 1000+ 条）下，V1.8 引入的
「看板数据导入/导出（备份恢复）」存在两方面不足：

1. **性能**：大接口量下导出/导入耗时偏高。根因不在磁盘 I/O，而在
   ① 导出时对同一份请求/响应报文反复调用 `toByteArray()` 全量序列化 +
   美化缩进；② 导入恢复时逐行走“实时抓流”的表格增量管线（每行都做
   签名/去重/自动注释与 EDT 表格事件）。
2. **可用性**：导入覆盖恢复后，若残留的表格过滤条件（搜索词 / 只看标记 /
   状态白名单勾选不全）与恢复数据的会话/状态列不完全匹配，会出现
   「表格已过滤：0/N 可见条目」、数据看似丢失其实被滤掉的情况。

另外，清除表格是破坏性操作（清空全部会话结果且无法撤销），缺少防误触
确认。

本次迭代针对以上三点做优化与修复。

## 二、改动明细

### 1. 清除表格二次确认（防止误点清空看板）

- 「清除表格」按钮点击后，若看板存在数据（行数 > 0）先弹出确认框：
  “确定要清除当前看板全部 N 条数据吗？此操作会清空所有会话的检测结果
  且无法撤销，请先导出备份。”
- 点「确定/OK」才执行清除；点「取消」或关闭对话框则直接中止，不清除。
- 空表（无数据）时不弹窗，避免无谓打扰。
- 二次确认仅在**按钮入口**生效；由程序触发的内部清表路径
  （如修改会话设置时的清表）本身已有“数据将丢失”提示，不重复弹窗。
- 涉及代码：`CenterPanel.clearTablePressed()`（开头新增 `JOptionPane`
  确认，`OK_CANCEL_OPTION` + 警告图标，try/catch 兜底防弹窗异常阻塞清除）。

### 2. 修复：导入备份后看板数据被过滤隐藏（0/N 可见）

根因有两层，均已修复：

- **过滤白名单兜底（`CustomRowSorter.include()`）**：
  - 兜底 1：四个状态白名单（SAME/SIMILAR/DIFFERENT/NA）一个都未勾选时
    不再“全不勾反而全滤”，而是放行全部。
  - 兜底 2：某行没有任何 `BypassConstants` 状态值（如备份中的会话名
    未匹配当前配置、状态列全空）时，跟随 NA 白名单照常显示，而不是被
    滤成 0 可见。
- **导入后重置过滤（`CenterPanel.resetBoardFilterAfterImport()`）**：
  覆盖恢复成功后自动清空搜索词、取消“只看标记/去重”、并把四个状态
  白名单恢复为全勾，避免残留过滤条件把刚恢复的数据滤成不可见。

### 3. 性能优化：备份导入/导出大接口量提速

- **`HttpExchange` 惰性字节缓存**：`getRequestBytes()/getResponseBytes()`
  由“每次 `toByteArray()` 重新全量序列化”改为 `volatile` 字段缓存一次
  结果（montoya 的请求/响应对象不可变，缓存安全）。消除导出时对同一
  报文反复序列化的最大 CPU 开销。
- **导出去掉 pretty-print**：`DataExporter.createSnapshot()` 不再
  `setIndent("  ")`，输出紧凑 JSON。JSON 结构不变，旧备份仍可读；
  `JsonReader` 流式解析不依赖缩进，导入兼容新旧两种格式。
- **导入批量重建（`RequestTableModel.bulkRebuildForRestore`）**：
  `DataImporter.restore()` 改为两段式——先 `buildRow()` 构造每行
  `OriginalRequestResponse` 并写入各 Session（不再逐行调用表格增量
  add），收集到列表后再一次性交给 `bulkRebuildForRestore()` 建立全部
  索引/签名/重复代表与自动注释，仅触发一次表格刷新。绕过逐行走实时
  去重 + 批量 EDT 事件的高昂成本。
  - 前提约束：调用前已清空看板、行已分配新 ID 且按 ID 升序（与实时
    增量语义一致），故恢复后重复折叠/代表 ID 与实时行为保持一致。

## 三、涉及代码

| 文件 | 改动 |
|---|---|
| `src/com/protect7/authanalyzer/gui/main/CenterPanel.java` | `clearTablePressed()` 加二次确认；新增 `resetBoardFilterAfterImport()`（导入后重置过滤）；导入流程接入批量恢复 |
| `src/com/protect7/authanalyzer/gui/util/CustomRowSorter.java` | `include()` 新增“四白名单全不勾放行”与“行内无状态值跟随 NA 显示”两层兜底 |
| `src/com/protect7/authanalyzer/gui/util/RequestTableModel.java` | 新增 `bulkRebuildForRestore(List)`（批量建索引/签名/自动注释，单次刷新）；新增 `List` import |
| `src/com/protect7/authanalyzer/montoya/HttpExchange.java` | `getRequestBytes()/getResponseBytes()` 增加惰性字节缓存字段 |
| `src/com/protect7/authanalyzer/util/DataExporter.java` | `createSnapshot()` 去掉 `setIndent("  ")`，输出紧凑 JSON |
| `src/com/protect7/authanalyzer/util/DataImporter.java` | `restore()` 重构为 `buildRow()` + `bulkRebuildForRestore()` 批量恢复 |

## 四、验证

- 4 处改动后 javac（`-source 8`，与 pom 编译目标一致）编译通过，无告警。
- 打包产物已更新（增量写入相关 class 及全部内部类）：
  - `target/auth_analyzer_modify-1.9.jar`
  - `target/auth_analyzer_modify-1.9-jar-with-dependencies.jar`（Burp 加载用）
- javap 反编译核对产物：`RequestTableModel.bulkRebuildForRestore`、
  `HttpExchange` 缓存字段、`DataImporter.buildRow`、
  `CenterPanel.resetBoardFilterAfterImport`、`clearTablePressed` 内
  `showConfirmDialog`、`CustomRowSorter.include()` 的 `instanceof
  BypassConstants` 兜底均已写入。
- 说明：montoya 报文构造依赖 Burp 运行时（headless 无
  `ObjectFactoryLocator`），无法本地跑“导出→导入→逐字段比对”的端到端
  用例。建议首次加载后在 Burp 内实测：建 2~3 个 Session → 放行数百条
  请求（含大响应体）→ 导出备份 → 清表/改乱过滤 → 导入 → 核对行数全
  部可见、状态列正确、耗时是否下降。

## 五、兼容性

- 备份 JSON 结构保持 V1.8 定义不变；旧（V1.8）备份可直接导入。
- 恢复语义不变：仍是“解析校验全部通过后才清空重建，失败自动中止、
  不清空现有数据”。
- 清除表格确认、过滤兜底、性能优化均向后兼容，不影响既有实时抓流链路。
