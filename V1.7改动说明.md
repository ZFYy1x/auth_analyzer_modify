# V1.7 改动说明

> 迭代主题：看板功能搜索支持 `{%}` 通配符变量
> 日期：2026-09-02

## 一、需求背景

在越权测试看板（表格）中搜索接口路径时，不同请求的动态参数段各不相同，
例如同一资源的不同 ID：

```
/api/reports/authorized/100/html
/api/reports/authorized/200/html
/api/reports/authorized/300/html
```

旧版搜索只支持普通子串匹配，无法用一个搜索词覆盖上述动态路径。
本次迭代引入 `{%}` 通配符：**`{%}` 代表任意字符（可多个、可含空串）**，
用于统一检索一类动态路径。

## 二、功能说明

### 1. 语法示例

| 搜索词 | 可命中示例 | 说明 |
|---|---|---|
| `/api/reports/authorized/{%}/html` | `/api/reports/authorized/123/html` | `{%}` 匹配任意一段字符 |
| `/api/{%}/authorized/{%}/html` | `/api/v1/authorized/99/html` | 支持多个 `{%}` 同时使用 |
| `/api/{%}.jsp` | `/api/report.jsp` | `{%}` 可出现在任意位置 |
| `report` | `my_report_1` | 不含 `{%}` 时保持普通子串搜索 |

### 2. 设计细节

- **字面量安全**：搜索词中除 `{%}` 外的部分一律按字面量匹配，
  `{%}` 编译为正则 `.*`；用户无需（也不应）输入正则元字符，
  例如 `a.c` 仍按字面 `a.c` 匹配，不会把 `.` 当作通配符。
- **生效范围**：勾选的「路径 / 请求 / 响应」三种搜索目标全部支持。
- **反向搜索兼容**：`{%}` 模式与「反向搜索」复选框可同时使用。
- **匹配语义**：`{%}` 可匹配任意字符，包括空串与 `/`。
- **性能**：搜索词到正则的编译结果按搜索词缓存，重复过滤无额外开销；
  不含 `{%}` 的搜索仍走原有 `contains` 快速路径。

## 三、涉及代码

| 文件 | 改动 |
|---|---|
| `src/com/protect7/authanalyzer/gui/util/CustomRowSorter.java` | 新增 `matchesSearch()` / `getWildcardPattern()`（静态方法 + 模式缓存）；路径/请求/响应三处匹配统一改走新方法 |
| `src/com/protect7/authanalyzer/gui/main/CenterPanel.java` | 搜索框 placeholder 与 tooltip 增加 `{%}` 用法提示 |

## 四、验证

- 语义用例 13 项全部通过（单/多通配符、空串匹配、字面量元字符、子串兼容、反向场景）。
- javac（`-source 8`，与 pom 编译目标一致）编译通过。
- 打包产物已更新：
  - `target/auth_analyzer_modify-1.7.jar`
  - `target/auth_analyzer_modify-1.7-jar-with-dependencies.jar`（Burp 加载用）

## 五、使用方式

1. Burp → Extensions → Add，加载 `auth_analyzer_modify-1.7-jar-with-dependencies.jar`
2. 看板搜索框输入含 `{%}` 的模式，点击「搜索」（或回车）
3. 结果自动过滤出所有匹配动态路径的请求

## 六、已知事项

- 本机 `mvn` 启动器异常（plexus classworlds 类加载失败），本次采用
  `javac + ~/.m2/repository 本地依赖` 直接编译，并用 `jar uf` 增量更新产物；
  后续整包构建需先修复 Maven 环境。
